partition lvaos-boot {
	size = ${BOOT_SIZE}
	partition-type = 0xc
	bootable = "yes"
	image = "boot.vfat"
}

partition lvaos-kernel0 {
	partition-type = 0x83
	size = ${KERNEL_SIZE}
	image = "kernel.img"
}

partition lvaos-system0 {
	partition-type = 0x83
	size = ${SYSTEM_SIZE}
	image = ${SYSTEM_IMAGE}
}

partition lvaos-kernel1 {
	partition-type = 0x83
	size = ${KERNEL_SIZE}
}

partition lvaos-system1 {
	partition-type = 0x83
	size = ${SYSTEM_SIZE}
}

partition lvaos-bootstate {
	partition-type = 0x83
	size = ${BOOTSTATE_SIZE}
}

partition lvaos-overlay {
	partition-type = 0x83
	size = ${OVERLAY_SIZE}
	image = "overlay.ext4"
	forced-primary = "yes"
}

partition lvaos-data {
	partition-type = 0x83
	image = ${DATA_IMAGE}
	forced-primary = "yes"
}
